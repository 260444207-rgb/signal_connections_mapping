# OpenClaw Loop Progress

This file is human-readable working memory for the supervisor and workers.

Every iteration should append:

```text
iteration id
selected task
agent result
evaluator result
state transition
next action
```
